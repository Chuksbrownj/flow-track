import { PagePlaceholder } from "@/components/app/page-placeholder";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Organisation and programme information."
    />
  );
}
